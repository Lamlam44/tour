package com.project.tour.DTO;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class PaymentRequestDTO {
    // This DTO is now empty, but we'll keep it for potential future use
    // and to maintain consistency in the controller.
}
